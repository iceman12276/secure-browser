# Node
node_modules/
out/
dist/
*.log
.DS_Store

# electron-vite / vite
.vite/

# TypeScript incremental build info
*.tsbuildinfo

# Rust
core/target/
target/

# napi build artifacts
core/*.node
core/index.js
core/index.d.ts

# Playwright
test-results/
playwright-report/
.cache/

# UX acceptance — transient per-run artifacts. The spec, ACCEPTANCE.md, and the
# curated step screenshots (01-15) are the durable record; these regenerate each run.
tests/ux/results.json
tests/ux/screenshots/NET-*.png
tests/ux/screenshots/OMNI-*.png

# Visual companion scratch (superpowers brainstorming)
.superpowers/

# Design handoff bundle (reference only)
_handoff/
secure-browser-handoff.zip
