/**
 * The page a fresh tab opens to — both the first tab on launch and the tab the
 * "+" button creates when no URL is supplied. DuckDuckGo's search box doubles as
 * the home page and matches the omnibox's default search engine, so a new tab is
 * immediately useful (type and search) rather than a static placeholder.
 */
export const HOME_URL = 'https://duckduckgo.com';
